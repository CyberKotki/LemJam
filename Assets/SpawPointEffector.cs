using UnityEngine;
using UnityEngine.UI;
using DG.Tweening;

public class SpawPointEffector : MonoBehaviour
{
    [SerializeField] Image buffor;
    [SerializeField] float bufforDuration;

    public void OnSpaw()
    {
        //DOTween.To(() => );
    }
}
